# List Operations

# Q-8: Quiz (Len Quiz)

# What is the value of len(p) after running the following code:
p = [1, 2]
p.append(3)
p = p + [4, 5]
len(p)
# Ans: 5
print(len(p))

# Q-9: Quiz

# What is the value of len(p) after running:
p = [1, 2]
q = [3, 4]
p.append(q)
# len(p) → ?
print(len(p))
